[build-system]
requires      = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sourcelib"
version = "0.0.0"
description = ""
readme = "README.md"
authors = [{ name = "Mart van Rijthoven", email = "mart.vanrijthoven@gmail.com" }]
license = { file = "LICENSE" }
requires-python = ">=3.8"

[project.urls]
Homepage = "https://github.com/martvanrijthoven/source-lib/"
